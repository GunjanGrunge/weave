{
  "name": "story-functions",
  "private": true,
  "type": "module",
  "engines": {
    "node": "22"
  },
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "eslint .",
    "lint:seams": "node scripts/assert-lint-rules.mjs",
    "test": "vitest run",
    "verify": "npm run lint && npm run lint:seams && npm run build && npm run test"
  },
  "dependencies": {
    "@google/genai": "^2.13.0",
    "@langchain/langgraph": "^1.4.8",
    "firebase-admin": "^14.2.0",
    "firebase-functions": "^7.3.0"
  },
  "devDependencies": {
    "@eslint/js": "^9.32.0",
    "typescript": "^5.8.3",
    "typescript-eslint": "^8.56.1",
    "vitest": "^3.2.4"
  }
}
